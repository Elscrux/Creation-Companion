<local:LoadingViewBase x:Class="CreationEditor.GUI.Views.Controls.LoadingView"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:local="clr-namespace:CreationEditor.GUI.Views.Controls"
                 xmlns:models="clr-namespace:CreationEditor.GUI.Models"
                 mc:Ignorable="d" d:DataContext="{d:DesignInstance models:LoadingItem}"
                 d:DesignHeight="300" d:DesignWidth="300">
    <local:LoadingViewBase.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </local:LoadingViewBase.Resources>
    <StackPanel>
        <TextBlock FontSize="16" Text="{Binding LoadText}"
                   TextAlignment="Center"/>
        <ProgressBar Minimum="0" Maximum="1" 
                     Value="{Binding LoadProgress}"
                     Height="10"
                     Visibility="{Binding AnyProgress, Converter={StaticResource BooleanToVisibilityConverter}}"/>
    </StackPanel>
</local:LoadingViewBase>
