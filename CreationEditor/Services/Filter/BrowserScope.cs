namespace CreationEditor.Services.Filter;

public enum BrowserScope {
    /// <summary>
    /// The whole environment is evaluated
    /// </summary>
    Environment,
    /// <summary>
    /// Only the active mod is evaluated
    /// </summary>
    ActiveMod,
}
